{
  "name": "acala-subquery-example",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen",
    "update-types": "node ./scripts/updateTypes.js"
  },
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Brett Kolodny",
  "license": "Apache-2.0",
  "devDependencies": {
    "@acala-network/api": "^2.3.1",
    "@acala-network/types": "latest",
    "@polkadot/api": "^5.7.1",
    "@polkadot/typegen": "^6.1.1",
    "@subql/cli": "latest",
    "@subql/types": "latest",
    "js-yaml": "^4.1.0",
    "typescript": "^4.1.3"
  }
}
